services:
  legalcore:
    image: legalcore
    build:
      context: packages/legal-core
      dockerfile: ./Dockerfile
    environment:
      NODE_ENV: production
    ports:
      - 3000:3000
